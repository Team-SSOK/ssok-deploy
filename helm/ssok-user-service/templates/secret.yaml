apiVersion: v1
kind: Secret
metadata:
  name: {{ include "ssok-user-service.fullname" . }}-secret
  labels:
    {{- include "ssok-user-service.labels" . | nindent 4 }}
type: Opaque
data:
  {{- with .Values.user.db }}
  {{- toYaml . | nindent 2 }}
  {{- end }}