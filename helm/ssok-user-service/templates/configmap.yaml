apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ssok-user-service.fullname" . }}-cm
  labels:
    {{- include "ssok-user-service.labels" . | nindent 4 }}
data:
  {{- with .Values.user.config }}
  {{- toYaml . | nindent 2 }}
  {{- end }}