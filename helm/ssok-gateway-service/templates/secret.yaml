apiVersion: v1
kind: Secret
metadata:
  name: {{ include "ssok-gateway-service.fullname" . }}-secret
  labels:
    {{- include "ssok-gateway-service.labels" . | nindent 4 }}
type: Opaque
data:
  {{- with .Values.gateway.db }} # 이후 secret 들어오면 그때 사용 예정
  {{- toYaml . | nindent 2 }}
  {{- end }}