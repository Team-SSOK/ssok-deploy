apiVersion: v1
kind: Secret
metadata:
  name: {{ include "ssok-transfer-service.fullname" . }}-secret
  labels:
    {{- include "ssok-transfer-service.labels" . | nindent 4 }}
type: Opaque
data:
  {{- with .Values.transfer.db }}
  {{- toYaml . | nindent 2 }}
  {{- end }}