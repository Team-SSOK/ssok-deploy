apiVersion: v1
kind: ConfigMap
metadata:
  name: ssok-notification-service
  namespace: ssok
data:
  application.yml: |
    server:
      port: 8080
    spring:
      application:
        name: ssok-notification-service
      cloud:
        kubernetes:
          config:
            enabled: true
          secrets:
            enabled: false
          reload:
            enabled: true
      datasource:
        url: jdbc:postgresql://172.21.1.22:5432/mydb
        username: mydb
        password: 1234
        driver-class-name: org.postgresql.Driver
      data:
        redis:
          host: 172.21.1.22
          port: 6379
      jpa:
        hibernate:
          ddl-auto: update
        properties:
          hibernate:
            format_sql: true
            show_sql: true
        database-platform: org.hibernate.dialect.PostgreSQLDialect
    bank:
      server:
        url: http://ssok-account-service
    feign:
      client:
        config:
          default:
            connectTimeout: 5000
            readTimeout: 5000
            loggerLevel: full
      hystrix:
        enabled: true
    logging:
      level:
        kr.ssok.userservice.client: DEBUG
    management:
      endpoints:
        web:
          exposure:
            include: "*"
      endpoint:
        health:
          show-details: always
    aligo:
      SMS_API_URL: https://apis.aligo.in/send/
      API_KEY: eu0x6jq8ohfldehok2vvbecg0exhnrle
      USER_ID: kingmandoo95
      SENDER: 01077948797